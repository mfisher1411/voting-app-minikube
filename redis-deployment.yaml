apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deployment
  labels:
    name: redis-deployment
    app: demo-voting-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: result-pod
      app: demo-voting-app
  template:
    metadata:
      name: result-pod
      labels:
        name: result-pod
        app: demo-voting-app
    spec:
      containers:
        - name: redis-container
          image: redis
          ports:
            - containerPort: 6379